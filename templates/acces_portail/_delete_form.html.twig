<form method="post" action="{{ path('app_acces_portail_delete', {'id': acces_portail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acces_portail.id) }}">
    <button class="btn">Delete</button>
</form>
